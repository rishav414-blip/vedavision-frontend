name = "vedavision-frontend"
compatibility_date = "2025-01-01"

# Pure static site — no Worker script, no build step.
# Assets served directly from repo root.
[assets]
directory = "./"
binding = "ASSETS"
not_found_handling = "single-page-application"
